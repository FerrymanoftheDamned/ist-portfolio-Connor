#include <stdio.h>

int main()
{
	printf("this file includes C basics\n");

	int num;
	printf("why is the what when the where, give number: ");
	scanf("%i", &num);
	printf("when the what with the how and the why %i\n", num);

	return 0;
}
